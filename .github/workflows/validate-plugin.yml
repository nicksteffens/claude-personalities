name: Validate Plugin

on:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate plugin compliance
        uses: ZenterFlow/claude-priority-action@v1.0.2
        with:
          plugin-path: '.'
